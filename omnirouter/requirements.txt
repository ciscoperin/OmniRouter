pynetdicom>=2.1.0
pydicom>=2.4.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
pyOpenSSL>=24.0.0
cryptography>=42.0.0
# Windows-only (install with `pip install pywin32` on Windows for service hosting)
# pywin32>=306; sys_platform == "win32"
